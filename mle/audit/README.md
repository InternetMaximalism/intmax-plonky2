# 現行 wire-v3 Lean 監査

対象: submodule main `becfe98e37c76e62f02f1aa7a417c7b06840db67`。
実装本体は `b569e0d7`、プロトコルは `MLEWHIR3` です。
ファイル名の `V2` はAPI世代名で、旧wire v2を受理する意味ではありません。

**状態: 部分的な実行可能モデルと決定論的性質を証明済み。
実装全体のLean化・Rust/Solidityとの形式的同値性・暗号健全性の証明は未完了です。**

旧監査を現行実装の証明として流用しません。通常の `lake build` とCIは現行
`Audit.lean` を検査します。7月のモデル・根の個数公理・旧所見は
[歴史資料](HISTORICAL-README.md)および明示的な `HistoricalAudit` targetに分離しています。

## 追加した現行モデル

| モジュール | 実装に対応する範囲と証明 |
|---|---|
| [Arithmetic](Audit/Wire3/Arithmetic.lean) | Goldilocks/Ext3の具体的mod演算、canonical値の保存。素数性・拡大体の既約性・逆元・機械語実装の証明ではない |
| [Packed](Audit/Wire3/Packed.lean) | LSB-first隣接fold、任意のゼロ末尾paddingとの結果一致、配列長、全座標反転 |
| [Transcript](Audit/Wire3/Transcript.lean) | 実byteのLE符号化、同一の旧digestの下でのhash前tag/payload一意性、counterと両round/全claim吸収順 |
| [Compact](Audit/Wire3/Compact.lean) | 実byteの外側grammar、canonical limb、読取り境界、headerと完全消費。完全Proof decoderではない |
| [Sumcheck](Audit/Wire3/Sumcheck.lean) | 意味的round関数に対する決定論的collision reduction。確率評価や実coefficientsとの全接続ではない |
| [Verifier](Audit/Wire3/Verifier.lean) | 設定/shape境界、coupled round履歴、3 roots・2 points・6 claims、terminal条件とWHIR観測への束縛 |
| [Connections](Audit/Wire3/Connections.lean) | canonical subtypeとFin3表現の双方向変換、実Packed.foldをVerifierへ接続 |
| [WhirTerminal](Audit/Wire3/WhirTerminal.lean) | WHIR終端query/最終多項式比較の限定slice。全WHIRやMerkle暗号健全性ではない |
| [WhirFinal](Audit/Wire3/WhirFinal.lean) | 同一finalVectorを最終sumcheck・逆順fold・非零・全線形形式・EOFへ接続。前段WHIR/decoder等は観測 |
| [Merkle](Audit/Wire3/Merkle.lean) | 実byteの層別multiproof処理、cursor範囲、同じindex/depthのpathに対する圧縮衝突への還元 |
| [Norm](Audit/Wire3/Norm.lean) | formal norm/adjugate、helper/logUp・PI集計、固定Configと同一statementを使う具体terminal |
| [Gates](Audit/Wire3/Gates.lean) | 全14familyの設定検証と基礎6family、selector/Horner。単独の旧部分dispatcherは追加8family未対応だが、統合入口では使わない |
| [Algebra](Audit/Wire3/Algebra.lean) | 具体mod演算から交換・結合・分配・加法逆元を証明し、eq/subgroup/squareの異なる式を接続 |
| [GatesAdditional/Coset](Audit/Wire3/GatesAdditional.lean) | 残る6familyの全評価式、Cosetの実124定数、chunk/Ext2/bit順序と具体正常例 |
| [Poseidon/Constants](Audit/Wire3/Poseidon.lean) | Poseidon/MDSの全評価、123/24制約、実1023定数、全30roundを通る正常witnessとRust標準出力 |
| [GatesComplete](Audit/Wire3/GatesComplete.lean) | 全14familyの具体dispatcher。valid設定・入力長なら全familyが実計算結果を返すことを証明 |
| [Integrated](Audit/Wire3/Integrated.lean) | packed/norm/eq/全gateの4観測を具体化。checked preflightで不正設定やdecoder失敗のzero fallback受理を防ぐ部分統合入口 |
| [NormIdentity](Audit/Wire3/NormIdentity.lean) | 実Ext3係数上のformal adjugate恒等式、逆元candidateとの積、WHIR sum-formとPacked difference-formの同値 |
| [ModularPower](Audit/Wire3/ModularPower.lean) | 実binary冪乗の正確値とfuel条件、inverse成功時の積をnorm^(p−1)へ還元。一般Fermatは後段GoldilocksFoundationで解消 |
| [Spongefish](Audit/Wire3/Spongefish.lean) | 内側WHIRのraw byte hash chain、BE counter、120byte challenge、canonical read、PoW、hint Vec長。Hashの安全性は仮定しない |
| [WhirInitial](Audit/Wire3/WhirInitial.lean) | 実root/own-OOD/全claim/cross-OOD読取りから2種のRLCと初期sumを生成し、同一データ・読取り量・checked maskを証明 |
| [WhirFinalSpongefish](Audit/Wire3/WhirFinalSpongefish.lean) | 最終sumcheckの3観測を実byte処理へ置換。1roundあたり48/56byte、120byte challenge、受理時の正確なsuffix長とhint EOF |
| [WhirPrefix](Audit/Wire3/WhirPrefix.lean) | 初期処理→最初のsumcheckを同一source・初期sum・spongeで接続。中間以降の全WHIR接続ではない |
| [PiSharedBits/PiCache](Audit/Wire3/PiCache.lean) | 実OR/XOR共通bit分離・逆順検索・重複行合算・eta末尾更新省略を含むキャッシュと直接PI和の同値 |
| [GoldilocksCertificate](Audit/Wire3/GoldilocksCertificate.lean) | 体証明で使う具体べき乗/gcd証明書。単独では素数性を結論せず、GoldilocksFoundationのLucas証明に渡す |
| [MerkleExtraction](Audit/Wire3/MerkleExtraction.lean) | 実multiproof成功から全leafの深さ付きpathを抽出。同root/index/depthの開示を一致または具体的hash衝突へ還元 |
| [WhirSampling](Audit/Wire3/WhirSampling.lean) | byteごとの実hash/counter・BE query・maskと挿入sort基準版。実indexed sortとの接続は後段WhirSamplingExecutionを参照 |
| [WhirRows](Audit/Wire3/WhirRows.lean) | Vec要素数→元の連続hint行→raw hash→既存Merkleのcursor接続。canonical decoderは実行順を保つため別操作 |
| [WhirRowBinding](Audit/Wire3/WhirRowBinding.lean) | 実openGroupの同root/depth/index行についてraw bytes・同Layoutの復号値・同weightsの内積が一致するか、具体的hash衝突があることを証明 |
| [FermatBridge](Audit/Wire3/FermatBridge.lean) | 明示したFermat等式から実inverseの左右逆元・消去・除算を証明。後段Foundation/Normが一般Fermatと非零norm条件を解消 |
| [WhirSchedule](Audit/Wire3/WhirSchedule.lean) | 実folding検査の投影から全変数の分割・各roundの残suffix・underflowなし・最終サイズを証明。domain/点を含む完全設定検査ではない |
| [GoldilocksFoundation](Audit/Wire3/GoldilocksFoundation.lean) | 具体証明書と証明済みLucas基準からpの素数性、一般Fermat、立方根2の不存在を導出。新しい体公理を置かない |
| [GoldilocksNorm](Audit/Wire3/GoldilocksNorm.lean) | 実normが0 iff canonical Ext3が0。全非零入力で実WhirFinal.inverseが成功し、左右の積が1になることを証明 |
| [GoldilocksExt3Field](Audit/Wire3/GoldilocksExt3Field.lean) | 実演算・実inverseを包む型にFieldを構成。標数p、三座標との全単射、要素数p³、Fermat/Frobeniusを証明 |
| [WhirPolynomial](Audit/Wire3/WhirPolynomial.lean) | 実constant-first Hornerと具体体上Polynomial.evalの一致。同長・不同の固定canonical vectorの一致点数≤長さ−1。query分布や適応選択の確率ではない |
| [GoldilocksLagrange](Audit/Wire3/GoldilocksLagrange.lean) | 実反復squareと2冪乗、guardとscalar範囲、実inverseを通るevalL0の有理式一致。x=1はsourceどおり失敗 |
| [WhirIntermediate](Audit/Wire3/WhirIntermediate.lean) | 初期prefixからnew root/OOD/PoW/前rootのraw Merkle/RLC/dot/constraint/sumcheckまで同じ実状態で接続。3 base roots→単一Ext3 root。設定投影・reference sortの境界は残る |
| [WhirDedup](Audit/Wire3/WhirDedup.lean) | 実indexed隣接重複除去の上書き・読書き境界・最終長と基準dedupの一致。quicksort自体の証明ではない |
| [WhirTail](Audit/Wire3/WhirTail.lean) | 同じ初期/中間実行から終端の両分岐、同一final vector、derived Context、最終claim・両EOFまで接続。3×1設定投影とsource refinementの境界は残る |
| [GoldilocksDomain](Audit/Wire3/GoldilocksDomain.lean) | 固定生成元7の位数p−1、k≤32で派生根の位数2^k、transpose指数と相異点の証明。任意shape-valid generatorや設定生成の証明ではない |
| [WhirQuadratic](Audit/Wire3/WhirQuadratic.lean) | 実sumcheckのc1復元/Horner、端点和、次数≤2と固定不同claimの一致点数≤2。比較側のtruth/FS/確率は別条件 |
| [WhirParameters](Audit/Wire3/WhirParameters.lean) | 実順の全typed設定guard、bound/deployment入口の区別、raw点の損失なしcanonical化と既存WhirInitial.validatedParamsへの接続 |
| [WhirDomainBridge](Audit/Wire3/WhirDomainBridge.lean) | 実domainPointを固定生成式へ接続。明示した生成元・次元条件下で単射と、同長・不同の固定vectorの一致query数≤長さ−1 |
| [WhirChallenge](Audit/Wire3/WhirChallenge.lean) | 実120byte/3×LE40byte還元の全単射と事象数上界。明示的uniform入力なら固定不同WHIR quadraticの一致確率≤2(ceil(2^320/p)/2^320)³。実FS分布は未証明 |
| [WhirConfigured](Audit/Wire3/WhirConfigured.lean) | 単一checked設定から全相へ投影し、実行成功と明示3×1条件から全到達相/終端Contextの形状を導出 |
| [WhirQuicksort/Correctness](Audit/Wire3/WhirQuicksortCorrectness.lean) | 実indexed scan/swap/partition/左右再帰をモデル化。一般終了・範囲外保存・整列・重複数保存から基準sortとの一致を証明 |
| [WhirSamplingExecution](Audit/Wire3/WhirSamplingExecution.lean) | 実sort→indexed compaction→raw queryを全WHIR両sampling箇所へ接続。結果・全状態・失敗の一致と単一checked設定入口の形状を証明 |
| [WhirDomainPower](Audit/Wire3/WhirDomainPower.lean) | 実64bit mask/low-bit/shift/mulmodのscalar loopと既存冪乗の一致。256bit指数で終了し、範囲内queryの実domainPointへ接続。compiler/EVM refinementではない |
| [OuterRound](Audit/Wire3/OuterRound.lean) | 外側の実係数和・inverse-two・Hornerを具体体多項式へ接続。端点和、次数、固定不同claimの一致点数、両laneの同じ更新を証明 |
| [OuterChallenge](Audit/Wire3/OuterChallenge.lean) | 外側の実3×32byte squeezeとcoupled commit後の6counterを接続。明示uniform law下の偏り込み固定多項式上界。実FSや両laneの積増幅は未証明 |
| [GatePolynomial/Basic/Mds](Audit/Wire3/GatePolynomial.lean) | 全14familyのzero-filter分岐除去と、7familyの実制約式・selector・固定alpha Hornerへの多項式評価一致と次数 |
| [GateCheckedPolynomial](Audit/Wire3/GateCheckedPolynomial.lean) | 7familyに限定し、実設定検査と両入力長から寄与次数≤q+1、明示affine重み後≤q+2。未対応familyはNONEを保持 |
| [NormPolynomial](Audit/Wire3/NormPolynomial.lean) | off-cube形式norm/adjugateと実寄与式からhelper≤4、eq重み込み行・有限和≤5、PI≤2。endpoint抽出/PI routing/送信係数は別境界 |
| [GateLoop/Random/TwelvePolynomial](Audit/Wire3/GateTwelvePolynomial.lean) | Exponentiation・BaseSum・Reducing2種・RandomAccessを追加し12familyの実次数と設定済み行寄与を証明。Poseidon/Cosetのsymbolic NONEは残る |
| [WhirRlc](Audit/Wire3/WhirRlc.lean) | 実geometric係数とrow dotを同じ多項式へ接続。固定同長不同vectorの一致点数≤n−1、明示uniform120byte上界、2RLCの実順序/別state・無消費分岐 |
| [GateCoset/Poseidon/AllPolynomial](Audit/Wire3/GateAllPolynomial.lean) | Poseidon4の全30round実状態・S-box・MDSから123制約・次数≤7、Coset13のchunk/reset/中間からD≥2で次数≤D。全14familyの実validateGateから寄与≤q+1、affine重み後≤q+2。gate truth/endpoint由来は別境界 |
| [GateAggregate/SuffixPolynomial](Audit/Wire3/GateSuffixPolynomial.lean) | 実combineRows全行の順序付き集約を1多項式へ接続し次数q+1、実設定包絡q≤8から重み後≤10。2s/2s+1読取り・同一challenge補間・Boolean suffix和も同じ多項式。Rust slot-first順序との可換は未証明 |
| [OuterInterpolation/Total](Audit/Wire3/OuterInterpolationTotal.lean) | coefficients.rsの実Gauss消去（自然node・pivot交換なし・書込み前factor）を添字付きで模し、証明専用ghost RHSで到達pivot=∏(p−j)を同定。n≤pで全段成功・実inverse実行・非空全域、norm5/gate q+2へ特殊化 |
| [DenseMleIndexed](Audit/Wire3/DenseMleIndexed.lean) | ext3.rs bindの2i/2i+1 read-before-write・増順書込み・truncate・未書込suffix不変、Valid長=2^numVars、numVars>0 guard、全bindManyとPacked.layer/foldの一致、affine Norm橋 |
| [OuterInitial](Audit/Wire3/OuterInitial.lean) | 外側初期transcriptの16frame（circuit digest・raw PI・15語metadata・config digest・64/32byte識別子・2roots）とeta〜gate tauの順序/counter、d≤13で全checked squeeze成功。PI hash・VK意味論・Hash安全性は未証明 |
| [OuterAdapter](Audit/Wire3/OuterAdapter.lean) | 40byte内部snapshotの無損失decoder、実coupledRoundの6limb/counter、5claim+空第6cell→index domain→log/gate index列の実順序、checked外側loop=Verifier.roundStep、初期→WHIR contextのOption prefix。既存total Engineとの一致は明示CommitAgrees/SamplesAgree条件付き |
| [GateSlotAlgebra/Commutation/Round](Audit/Wire3/GateSlotRound.lean) | Rustのslot-first累積（accumulated[slot]+=filter·value、範囲外writeはnone）とforward alpha powersが実combineRows/evalCombinedと可換。current_roundのsuffix外側・integer内側のmutable累積がGateSuffixの整数ごとのsuffix和に一致し、Valid eq表でhalf=2^(numVars−1)。補間/DenseMle構成は別 |
| [NormDenseRound](Audit/Wire3/NormDenseRound.lean) | norm_logup.rsのround_sum_at（4 scratch vector・suffix loop・shift/mask PI loop）、evaluate_target_from_valuesの幅assert、current_roundの0..=5 sample→採用済み補間→定数省略、bindのprefix更新順とbindBuffer、prover stateのErr/panic/cache/Consistentを模し、送信5係数を実evaluateRoundが復元することを証明。構成データの由来・不正proverは別 |
| [OpenedClaimFold](Audit/Wire3/OpenedClaimFold.lean) | 採用済みPacked.fold/packedFold/bindManyのrow++index分割、pack_mlesの列優先padded table、Solidity cell/Rust slot対応（0-2はlog点、3-4はgate点、第6はnone）、native点=dense点の反転。明示的honest-prover仮定（claimed cell=各列のrow fold）下でexpectedClaimsの各cellがpadded tableのWHIR点での評価に一致。PCS binding/WHIR受理は別 |
| [GateTerminalBinding](Audit/Wire3/GateTerminalBinding.lean) | gate proverのbind_challenge（degree ensure→eq/全wire/全constantのbindBuffer→rounds/point push）とinto_proof_and_pointを模し、全変数bind後の各列=採用済みpacked fold、最終roundのeq·aggregateがGatesComplete.evalCombinedとVerifier.gateTerminalに一致（eq cell=eqEvaluation(gateTau,gatePoint)は明示仮定）。honest prover限定の受理claim一致も証明。回路truth/PCSは別 |
| [NormTerminalBinding](Audit/Wire3/NormTerminalBinding.lean) | 全変数bind後のnorm prover表（単一cell列）をNormTerminalInput（constants++sigma、routed wire cells++非routed tail、identity/sigma helper、PI値）へ対応させ、prover targetとVerifier側Norm.evaluate/checkedEvaluate/Engine.logTerminalの項ごとの一致を証明。各bound cell=採用済みpacked fold。eq/subgroup cellのtau由来・lambda/eta/wire-map由来は明示仮定、honest prover定理は表示 |
| [OuterClaimChain](Audit/Wire3/OuterClaimChain.lean) | honest proverのlog laneを実Verifier.roundStep/runRoundsで駆動し、各round後のlogClaim=直前表のroundValue(r_i)、次表のf'(0)+f'(1)=roundValue(r)（2≤remaining、読取り境界証明済み）、初期零claim⇔端点和零、intoProofAndPoint=verifierが消費したmessage/challenge、OuterAdapterとの一致。gate laneは明示仮定でparameterise。健全性は主張しない |
| [IntegratedTerminalChain](Audit/Wire3/IntegratedTerminalChain.lean) | OuterClaimChain/NormTerminalBinding/GateTerminalBinding/OpenedClaimFoldを採用済みIntegrated.verify/verify_success_checksへ接続。honest proverではlogTerminal=derivedRoundsの最終logClaim、gateTerminal=gateClaim、WHIR期待claim=padded table評価となり、決定論的検査は全て成立、残りは名前付き観測（config hash/envelope/deployment/shape/長さ/verifyWhir/7 challenge/normShape）のみ。Rust順（WHIR→norm）とSolidity順の受理同値。健全性は主張しない |
| [EqTableProvenance](Audit/Wire3/EqTableProvenance.lean) | 実eq表builder（tau順の外側loop・index内側loop・左側累積）を模し、各entry=Norm.booleanRowEq、低位bitが先にbindされることを採用済みbindBufferから証明。全点bind後のeq cell=Norm.eqEvaluation(tau,point)、subgroup cell=verifierの積形subgroupEvaluation。これによりnorm/gateのterminal定理からeq/subgroup由来の仮定を除去した系を与える。VK generator由来は明示仮定のまま |
| [PublicInputHashBinding](Audit/Wire3/PublicInputHashBinding.lean) | 実hash-no-padスポンジ（rate8/capacity4、overwrite、⌈len/8⌉chunk、4要素digest）を採用済みPoseidon上で具体化し、全30roundで基底体が保たれること・c0読出しが無損失であること、raw canonical preflight（256語上限と各語<P、fallbackなし）を証明。gate terminalのhash観測を具体関数へ置換し、hashLength仮定を除去 |
| [TranscriptProvenance](Audit/Wire3/TranscriptProvenance.lean) | 「engineの初期transcriptが実導出そのもの」という単一前提から、7 challengeがchallengesFromInitialと一致すること、log/gate tau列とgate alphaが導出値であること、各challengeがsourceのdigest/counter位置に載ることを証明。ObservationOnlyは12→9項へ減る。hashの性質は一切使わず、順序と受け渡しのみ |
| [VkSubgroupProvenance](Audit/Wire3/VkSubgroupProvenance.lean) | plonky2のtwo_adic_subgroupとVK生成元導出を模し、固定two-adic生成元の位数2^32を核証明書から確定。verifier_v2.rsの再計算検査は「VKのpowersが表の生成元の反復二乗である」ことと同値であることを証明し、EqTableProvenanceのSubgroupPowersProvenance仮定を解消する |
| [ConditionalSoundness](Audit/Wire3/ConditionalSoundness.lean) | 18個の暗号仮定を可視フィールドとして列挙し、受理かつlog laneのbad eventなしなら抽出表のendpointSum=0（IntegratedTerminalChainが仮定していたzeroSumの導出）。bad set濃度は195·⌈2^256/p⌉³以下。**gate laneは未証明、2^-184は外側sumcheck項のみでWHIRを含まず、系全体の健全性誤差ではない** |
| [GateDenseRound](Audit/Wire3/GateDenseRound.lean) | NormDenseRoundのgate版。current_roundを採用済みgridと補間で端まで模し、送信長=q+2、省略定数=verifierの半和復元、実evaluateRoundの再現、Consistentとround間端点恒等式、Boolean cube和の定義と第1roundでの一致を証明。多round帰納は未実施、cube和=0は主張しない（回路truth） |
| [OpeningBinding](Audit/Wire3/OpeningBinding.lean) | 同root/index/depthの2つの受理openingが一致することを、実行から計算した有限listへのhash単射性へ還元（自由変数ではない）。抽出ではなくbindingであることを明示し、限界も定理として証明する（不透明engineのWHIR検査は任意のcontextを受理、opening関係は3つのrootを差し替えても不変） |
| [ZeroCheckSemantics](Audit/Wire3/ZeroCheckSemantics.lean) | 採用済みeqによる多重線形拡張がcube上でgを補間し、非零値があれば恒等的に零でないこと、零点の密度≤n/\|F\|（Schwartz-Zippel、総次数≤n、帰納法は完遂）を証明。bad set外のtauでGateDenseRoundの各行値が0になる系を与える。badSetsとは合成しない（単一challenge対n組） |
| [GateClaimChain](Audit/Wire3/GateClaimChain.lean) | gate laneを実Verifier.roundStepで多round連鎖し、terminal橋を渡し、受理+gate lane bad eventなしから抽出表のcube和=0を導く。gate lane固有の上界(q+2)·degreeBitsも再具体化。**供給者への棄却力はまだない**（eq表の自由度、selector零化、`numGateConstraints=0`退化は仮定4で除外） |
| [AlphaZeroCheck](Audit/Wire3/AlphaZeroCheck.lean) | 行の集約をalphaの多項式として係数を同定（slot値そのもの）し、根の個数≤numGateConstraints−1≤122をenvelopeから導く。bad set外のalphaで各slot値が0、filterが非零のgateは制約自体が0。tauとalphaの両zero-checkの合成も採用済みchallenge上で証明。filter零の行は非拘束（正しい挙動） |
| [GateRejectionPower](Audit/Wire3/GateRejectionPower.lean) | 攻撃シナリオ4つを定理として提示し、eqの単位分解（行和=1）で行和ゼロ偽造を不可能にする。強化仮定は旧仮定が受理した偽造状態を棄却することを具体例で示す。**selectorの部分的零化は未解決**（constants由来＝抽出接合）。tauはtranscriptに束縛されていない |
| [GateDerivedRejection](Audit/Wire3/GateDerivedRejection.lean) | 棄却定理をtranscript導出のgate tau/alphaで再述し、§3–4と縮約仮定集合から自由なchallengeを消す（10フィールド→9、bound cell仮定は全表eq由来から導出）。残るchallenge側の仮定は導出tau/alphaの2つの`hgood`と、sumcheck roundのBadEventFreeの3つ。§2は例外で自由alphaを保持 |
| [CommitmentOrder](Audit/Wire3/CommitmentOrder.lean) | 適応性の決定論的半分。22 frameのprefixが採用済みrelationStateと一致し、3つのrootが13/15/19番目、最終rootとgate alpha/tau squeezeの間は固定domain tag 2つだけ。表の変更はprefixを変えるか具体的hash衝突を生む。確率的半分（B）は定数hashで反例が存在し、順序論法では届かないと定理で示す |
| [ChallengeUnionBound](Audit/Wire3/ChallengeUnionBound.lean) | tauのzero-check bad setをn個の独立digest tripleの積事象へ、行ごとのalpha bad setを2^n行のunionへ持ち上げ、外側sumcheck項と合わせて3つの質量の和を上界化。envelope極値で≤2^-172を厳密有理数計算で証明（余裕0.07 bit、envelopeぎりぎり）。**WHIR/Merkle項を含まず系の健全性誤差ではない**。3項は別々の標本空間上で結合則は未形式化 |
| [AttachedUnionBound](Audit/Wire3/AttachedUnionBound.lean) | ChallengeUnionBoundの自由パラメータ（n、g、係数族）を受理実行に束縛する。tau arityはdegreeBitsから導出、行値はgateValue、係数族は受理由来のslotCoefficients、制約数≤123は受理から導出。attached tuple＝transcriptのtau列（attached_tau_tuple_ofFn）。combinedBoundの4引数単調性を証明し、envelope下で≤combinedBound 13 8 13 123≤2^-172。attached_seamが計数上界と「全行で選択gateの制約が零」を1定理に結合。**数値はWHIR/Merkle項を含まず系の健全性誤差ではない**（余裕0.07 bit、制約129で破れる） |
| [ConstantsProvenance](Audit/Wire3/ConstantsProvenance.lean) | selector部分零化攻撃の由来鎖L1–L6。selector値←供給constants列←bound cell←gatePreprocessed claim←committed列のbound cell←preprocessed root下のopening←deployment pin（Verifier.shape、verifier_v2.rs 184-187、MleVerifierV2.sol 563）。攻撃定理partial_zeroing_forces_one_of_three：供給列とpinned列のbound cellが導出gate点で異なれば、root不一致（pinで閉鎖）∨抽出接合の破れ∨opening関係の破れ。具体的偽造例で旧仮定は検知せず新鎖が接合で捕捉することを示す。**残余**: bound cell一致は列同定より弱い（反例つき）、engine不透明性R1、opening関係R2、root→列写像R3 |
| [JointChallengeSpace](Audit/Wire3/JointChallengeSpace.lean) | 3つの質量を1つの標本空間に載せる。squeeze schedule（alpha、tau×d、外側log/gate round×d）で添字づけた`Draw d → DigestTriple`上の一様計数測度を定義し、座標事象・tau積事象の質量を計数で証明、真のdisjunction事象`jointBadEvent`の質量≤combinedBound（joint_union_bound）を証明。外側項は両laneの2d座標を被覆。seam `DrawEncodesRun`は座標符号化の主張で**hash仮定ではない**（reduceTriple全射により全hash・全受理実行で可住、補題つき）。Fiat–Shamir半分(B)「符号化drawがjointProbability分布」は未形式化で、合成定理は暗号仮定を一切持たない。外側bad setは実現challenge/messageに相対的で「prefix定数」ではない（tau/alphaのみ定数）。2^-173≤bound 13 8 13 123≤2^-172、**WHIR/Merkle項を含まず系の健全性誤差ではない** |
| [GatePointZeroCheck](Audit/Wire3/GatePointZeroCheck.lean) | ConstantsProvenanceの残余「bound cell一致は列同定より弱い」を導出gate点上のSchwartz–Zippelで閉じる。橋渡しcell(bindColumn col point)=extension col point（2^|point|≤|col|で成立、短い列では切り詰めとゼロ埋めで破れる反例つき）、一致集合=差分列の零点集合で密度≤n/|F|、一様質量≤tauTerm。合成定理: 列が異なり導出gate点が一致集合外なら抽出接合CellsMatchClaimsかopening関係OpensCommittedTableが破れる（受理下、root分岐はpinで閉鎖）。供給列幅は採用済みConsistent+EqCellBindingから、committed列幅はopening関係から導出。gate点列=gate laneのround challenge列を抽象engineで証明し、JointSpaceの第4族として質量≤tauTerm d。**残余**: 供給列は結論の2分岐以外でrootに束縛されない（col固定後の点という読みでのみ密度が意味を持つ）、round digest連鎖は具体engineのみ、R1–R3、half (B)未形式化 |
| [OuterSequentialConditioning](Audit/Wire3/OuterSequentialConditioning.lean) | JointChallengeSpaceの外側座標に対する逐次条件付け（Fubini）版union bound。有限積空間上の一般エンジン（Nodup座標列、round kのbad setは先行座標のみに依存：PrefixDependent）で質量≤Σ c_k/|A|を証明し、round message を先行challengeの関数とする`Lane`（`ownsNext`は交互走査 log 0, gate 0, log 1, … の位相ビット）で両laneに具体化。adaptive外側事象の質量≤outerTerm（prefix凍結版と同じ数値）、adaptive結合bad事象≤combinedBound、凍結版laneEventはエンジンの特殊例で採用済みjoint_union_boundを再導出。モデルの敵対者はgate messageが同roundのlog challengeを見られる分だけ実物より強い（安全側）。**依然として理想一様法則上で、half (B)は未形式化、WHIR/Merkle項を含まず系の健全性誤差ではない** |
| [InstalledWhirTail](Audit/Wire3/InstalledWhirTail.lean) | 残余R1（engine不透明性）。採用済みの手動WHIR tailモデル`WhirConfigured.run`をIntegrated.modelEngineの`whirTail`に据え付け（parseWhir等は観測のまま、transcript/roundは不変）、受理⇒具体tail実行成功・3 root=[pinned preprocessed, witness, normInverse]・評価値=5つのbound cellのpacked fold。配備プロファイル（numRounds≥1）では中間round 1で3 rootが開かれ、各root位置のrowが`(initialOpen wp).merkleDepth`でMerkle認証されること、2つの受理実行で同root・同leafのrowが一致するか衝突することを証明。finalsplit経路（numRounds=0、fixtureには無い）は`_finalsplit`として別掲。この engine は空hintを実際に棄却する（盲目engine反例はこのengineには当たらない）。**残余R1b**: 「認証済み round-1 行ブロックと3 rootのみから抽出する固定関数が存在する」（WHIR近接性+sumcheck健全性、確率的、未形式化；per-column形はR3を包含し、fold水準の変種も併記）。`wp`は自由パラメータ（ソースはprofile digestで固定、Leanでは観測） |
| [AdaptiveAgreementFamily](Audit/Wire3/AdaptiveAgreementFamily.lean) | GatePointZeroCheckの一致集合を座標ごとのSchwartz–Zippel（各座標のbad setは affine 制限の根で濃度≤1、恒等零の場合は数えない）に分解し、OuterSequentialConditioningの adaptive エンジンに載せる。供給列は最初の`cut`座標のみの関数`colOf (xs.take cut)`。質量≤tauTerm d、第5族込みの結合bad事象≤combinedBound+tauTerm d（envelope極値で≤2^-171）。**二分法の形でのみ閉じる**: 実現prefixを通るaffine slice上で差分列の拡張が恒等的に零（偽造者は1座標を見ただけで構成でき、形式的残余証人として証明。R2/R3の領分）か、点が質量≤tauTermの集合に入るか。cut=dでは事象は空、cut=0・定数colOfはGPZCの固定列の読みに一致。Lane水準の凍結比較: DrawEncodesRun下でlaneごとにOSCのadaptiveLaneEvent(frozenLane)⟺JCSのlaneEvent（両方向）。理想一様法則、half (B)未形式化、**WHIR/Merkle項を含まず系の健全性誤差ではない** |
| [PinnedWhirProfile](Audit/Wire3/PinnedWhirProfile.lean) | InstalledWhirTailの自由パラメータ`wp`をconfigに固定する。`CanonicalWhirProfileV2.validateCanonical`の4制約（1≤numVariables≤21、sessionId、パラメータdigest=表の行[0:32]、protocolId=行[32:96]）を`deploymentValid`の具体化として据え付け（他は観測のまま）、受理⇒復号したwpが正準検査を通り、tailが走ったレコードとソースの検査が検証したレコードが一致。**ソースは`inDomainSamples>0`をどこでも構文的に強制しない**（F3の確認）。正値性とnumRounds≥1は表の意味論仮定`TableIsCanonical`（rowDigest／rowSeparates＝keccak単射性の代役）の下、転記済み2行（n=10: samples 29・rounds 1、n=21: 29・4。fixtureと生成表のkeccak再計算で一致）に対してのみ導出し、`pinned_round_one_authentication`でhsamples/hroundsを消す。残り19行はdigestのみ。`c.whirEncoding=c₀.whirEncoding`は`_requirePinnedConfiguration`の理想化（keccak単射性の残余として明記）。keccak・VK immutable store・復号器のrefinementは未モデル |
| [InstalledIndexSampler](Audit/Wire3/InstalledIndexSampler.lean) | 観測だった`sampleIndices`を具体化。採用済みの検査付きsampler（OuterAdapter.sampleResult：used claimsを6 frame＝domain sep「pcs-constituent-claims-v3」・tag 6のlog pre/wit/norm・gate pre/wit・空vec・domain sep「pcs-constituent-index-v3」で吸収後、1つのdigestからcounter 3i（log）と3·indexBits+3i（gate）でsqueeze）をdecode上で全域化し、`installedSamplerEngine = installedEngine (resampled e thash)`（rfl）。index squeeze前のprefixがround状態++claim framesであること、index digestが等しければ状態digestとused claimsが等しいか具体的transcript衝突が出ること（tag 6 payload単射性＋採用済みabsorb_messages_bind_or_collide）、両laneの長さ=indexBits、ObservationOnlyDerivedのlogIndex/gateIndex項が定理化されることを証明。「indexはcellの後に引かれる」（R3の順序面）を`per_column_identification_hook`として定理化（確率的段階は未証明）。decode仮定は採用済みCommitAgrees下の実行とfixtureで可住。index点の座標空間はcoordinate statementのみで法則は主張しない。schedule上のblock番号はラベル（未導出）。commitRound・parseWhir等は観測のまま |
| [InstalledInitialTranscript](Audit/Wire3/InstalledInitialTranscript.lean) | sampler engineの上に`initialObservation := OuterInitial.derive`と`publicInputsHash := PublicInputHashBinding.hashNoPad`を据え付け、8フィールドが同時に具体化された1つのengineを与える（新規性は合成であり、`DerivedInitial`自体は採用済み`withInitial_derived`がrflで与えていた）。この engine では`DerivedInitial`が全vc, pで定理となり、下流36箇所の`hderiv`と`hsub`が無条件化。GateDerivedRejection・AttachedUnionBound・JointChallengeSpace合成定理・ConstantsProvenance・CommitmentOrderの主要定理を`hderiv`なしで再述（`hacc`はこの具体engine下の受理でより強い前提）。prefixはCommitmentOrderの22 frame、root 13/15/19、gate alpha/tauはprefix digestのcounter 9+3d／12+3d+3i。受理proofでは公開入力がSolidityの256語capと語ごとの<p検査を満たす。残る観測: commitRound・parseWhir・configurationHash・deploymentValid。prover側のchallenge一致は依然仮定。hashは任意の決定的関数（順序と配管の同一性であり、FS健全性ではない） |
| [InstalledRoundCommit](Audit/Wire3/InstalledRoundCommit.lean) | 観測だった`commitRound`を具体化。採用済みの検査付きround commit（domain sep「outer-sumcheck-lockstep-round-v3」・round index u64・log/gate messageのtag 6 frame・domain sep「…-challenges-v3」の5 frameを吸収し、round digestのcounter 0/3からlog/gate challengeをsqueeze。transcript_v2.rs 133-146／TranscriptV2.sol 247-261と照合）をdecode上で全域化し、`installedRoundEngine = installedSamplerEngine (recommitted e thash)`（rfl）。`CommitAgrees`を定理化し、決定された実行が採用済み`OuterAdapter.execute`と一致（残る仮定は初期transcript観測`hi`とdegreeBits≤13のみ）、IISのdecode仮定を無条件化、round digestが22 frame prefixの後に5 frameずつ連鎖しcounter 0/3から引かれることを定理化（GPZC RES-4とJCSのcounterラベルが定理に。sourceBlockの番号はラベルのまま）、laneのchallenge列は実digestの縮約、round messageの改変は具体的衝突を要する。**限界**: envelopeはwidth c=1のときindexBits=0を許し、その場合decode失敗経路（challenge全零）をindex長guardが通す。ただしhi＋degreeBits≤13の下では全snapshotがdecodeするので本モジュールの定理では到達不能（導出でないinitialObservationを持つengineでのみ生きる）。残る観測: initialObservation・publicInputsHash（InstalledInitialTranscriptが担当、合成は次段）・parseWhir・configurationHash・deploymentValid |
| [ComposedEngine](Audit/Wire3/ComposedEngine.lean) | 据え付け済みの全フィールドを1つのengineに合成する。modelEngineの4評価器＋whirTail（PinnedWhirProfileのpinned record）＋sampleIndices＋initialObservation＋publicInputsHash＋commitRound＋deploymentValid（pinnedDeployment）の10フィールドが具体化され、観測は`parseWhir`と`configurationHash`の2つだけ。各据え付けmoduleのengineとの同一性を5つのrflで示し（installerは可換）、InstalledRoundCommitの9定理が担ぐ初期transcript仮定`hi`をrflで消し、degreeBits≤13と形状仮定を受理から導く。要約定理`composed_acceptance_summary`は受理＋TableIsCanonical＋転記済み行から16連言（3 rootのround-1 Merkle認証、検査付き実行との一致、digest連鎖、used claims吸収後のindex squeeze、22 frame prefix、公開入力cap等）を導き、各連言は採用済み定理の適用のみ。抽象parseWhirは良いproofを落とせても誤ったrootを通せない（一貫性主張。deployment-pinnedなのはpreprocessed rootのみで、witness/normInverseはproof自身の宣言）。indexBits=0の失敗経路はこのengineでは到達不能。残余R1b/R2/R3・half (B)・keccak・TableIsCanonical（n∈{10,21}）は不変 |
| [InstalledWhirParse](Audit/Wire3/InstalledWhirParse.lean) | 観測だった`parseWhir`を具体化。採用済みWhirInitial.phaseInitial→WhirPrefix.run→WhirTail.runPrefix（初期phase・初期sumcheck・中間round）の射影として`concreteParseWhir`を定義（actualRoots=transcriptの1本目のroot、boundRoots=2本目、claims=mask 0x1fのreadClaims。whir_pcs.rs 1514-1556/1586-1614、SpongefishWhirVerify.sol 374/384-387と照合）。`verifyWhir`がprefix成功∧root一致∧claim一致∧tail受理と同値であること、root条件はprefix成功に含まれ冗長であること、transcriptバイト列がk·stride（stride=64+24·outDomainSamples·numVectors）と+32+24·(…)の位置にroot 2本を運ぶこと、先頭32バイトが先頭rootの符号化と異なる（または欠ける）transcriptは棄却されること、parseとtailが同じprefix実行を読むこと（tailRun=prefixRun.bind runTail、定義的）を証明。受理下でderivedContextの先頭rootはproofのpreprocessedRootでありshapeによりpinに一致。合成後に残る観測はconfigurationHashのみ。R1b/R2/R3・WhirContextのprotocolId/sessionId/parameters・`wp`の自由性は不変 |
| [IndexPointZeroCheck](Audit/Wire3/IndexPointZeroCheck.lean) | R3（per-column同定）の二分法をindex点上のSchwartz–Zippelで与える。橋渡し`Packed.fold cells idx = extension cells idx`（cells長≤2^|idx|、両者ともLSB先頭で反転なし。採用済みbound_column_is_packed_fold・GPZC橋渡し・sparse_fold_equals_zero_padded_foldの合成）、2つのcell族のpacked foldが一致するindex点の集合＝差分の零点集合で密度≤indexBits/|F|、一様質量≤tauTerm indexBits。R3定理: fold水準の関係（CellOpensFullTable／TailExtractsFoldLevel）の下、supplied cellsとcommitted列のopeningが一致する（HonestOpenings.openedの型そのもの）か、導出index点が明示的に上界された集合に入るか。bound cell i∈Fin 5で両lane（0–2はlog、3–4はgate）を被覆。同じ幅なら一致集合が全体⇔列が等しい（拡張のBoolean点補間による単射性）ので残余類は空、幅が異なる場合はゼロ埋めで隠れるため、capacityのみから末尾零を除いた同定を結論する変種も定理化。supplied幅はshape+envelopeから、committed幅は仮定（capacityは≤のみ）。cellsはindex squeeze前に吸収済み（IISの順序定理）だがhalf (B)は未形式化。IndexSpace上は結合形のみ。**R1b（fold水準）とR2は不変** |
| [ExplicitEngine](Audit/Wire3/ExplicitEngine.lean) | 抽象base engineを持たないverifierモデル。12フィールド全てが(gdec, hash, thash, khash, P, c₀)の関数（record literalへのrfl）。最後の観測`configurationHash`を`khash ∘ encodeConfig`として据え付け、`encodeConfig`はSolidityの`abi.encode(VerificationConfig)`（MleVerifierV2.sol 110-118／149・180／478-488、Rust mirror fixture_v2.rs 1974-2032）のフィールド順を鏡写しにする（offset語省略・gates/whir不透明・Nat vs uint256の3点で非バイト同一。「模倣」であり同一とは主張しない）。`deploymentValid`は`whirEncoding=c₀.whirEncoding ∧ profileOk`のみ（他の配備検証はSolidityではconstructor限定。**Solidity経路のみのモデルで、Rustのmle_verify_v2はkIs・subgroup powers・gates・circuit_config_digest・id類をcallごとに再検証**）。符号化は13フィールドcore上で単射（Bounded、受理から導出可）、config digest一致⇒core一致∨具体的khash衝突。受理⇒digest=pinのdigest、配備仮定`pin.configDigest=khash(encodeConfig c₀)`の下でcore c=core c₀∨衝突。PinnedWhirProfileの理想化仮定は衝突を除き定理に（weakExplicitEngineで冗長性を示す）。**残余**: circuitDigest・circuitConfigDigestは符号化されず（Solidityではimmutable、モデルでは攻撃者が選ぶtranscript入力＝half (B)の自由度）。gateRowsは非符号化だが受理でgates.lengthに固定。ComposedEngine／InstalledWhirParseの主要定理を転送。hash類は任意の決定的関数、WHIR tailは手動モデル、R1b/R2/R3・half (B)・回路真理・refinementは未解決 |
| [GateEvaluatorCoverage](Audit/Wire3/GateEvaluatorCoverage.lean) | gate評価器の被覆を定理として固定する。基礎Gatesの部分dispatcherはid 0,1,2,3,6,7のみだが、Integratedが使う完全dispatcher（GatesComplete.evaluateUnfiltered）は14 family全てを評価し、GateAllPolynomialが全familyの次数を上界する（coverage_status）。宣言表（制約数・必要wire・local constant・次数）をgate_ext3.rs 177-408／Plonky2GateEvaluatorExt3.sol 196-212から転記して採用済みrequirementsと一致することを証明（plonky2のnum_constraints/num_wires/degreeとも各family一致。次数はRustでもvalidate_gate_ext3_contextがGate::degree()で同じ不等式を検査し、Rust/Solidityに乖離なし）。形状検査付きの各family評価器（短い行を`getD`既定値で黙って評価せず`none`）と、検証済み行で完全dispatcherと一致する`evaluateGateFull`、alpha-Hornerの根上界がfamilyに依らないことの転送。**所見**: RandomAccessGateの124制約行は個別検査は通るが設定検証と envelope 123（MAX_GATE_CONSTRAINTS_V2＝Poseidonの制約数）で排除（Rustは幅でも排除）、BaseSumのbase 16以上は次数会計で設定不能、Lookup系はgate idを持たず（id≥14は棄却）。ソース模倣の意味論であり回路真理は主張しない |
| [SoundnessAssembly](Audit/Wire3/SoundnessAssembly.lean) | 決定論的結果と計数結果を1定理に組み立てる（glueのみ）。explicit engineの受理＋残余仮定の名前付き構造体`AssemblyResidue`（19フィールド：表の意味論と転記行、配備digestと有界性、fold水準のopening（TailExtractsFoldLevelから導出）、抽出列の高さとcommitted幅、GateDerivedRejectionの9フィールド、slot係数）＋「実digest drawが4族のouterBadEvent外」＋「実index drawがindexBadEvent外」から、全行の選択gate制約消失、bound cellのper-column同定、pinned profile行、`core c = core c₀ ∨ khash衝突`を導く`explicit_good_draw_assembly`。質量: outerBadEvent∪agreement≤combinedBound+tauTerm d（envelopeで≤2^-171）、index事象≤2·tauTerm bits（bits≤8で≤2^-187）。2つの標本空間は別で加算しない。**経緯**: 初稿はindex bad eventが無ガード（列が等しければ全点）で結論が仮定と矛盾し、偽造前提が残余に紛れており、レビューがLeanでFalseを導いたためFAIL。ガード付き（CellsDifferOnCube）に修正し、constants攻撃は条件付き系`constants_forgery_forces_join_or_opening_off_bad_set`に分離、再レビューで反駁が型検査を通らないこと・正直な抽出ではouterBadEventが空になることを確認。**hacc∧残余の同時充足可能性は未提示**（explicit engineで受理されるproofの構成が要る）。名前は「soundness」ではなく「good-draw」：回路真理・WHIR近接性・hash安全性・R1b/R2・half (B)は結論に含まれない |
| [CanonicalProofCheck](Audit/Wire3/CanonicalProofCheck.lean) | ExplicitEngineの残余（circuitDigest・circuitConfigDigest）を閉じる。Solidityがcallごとに走らせる`_requireCanonicalProof`（MleVerifierV2.sol 551-587）の10比較のうち、Leanに対応がなかったのはproofのcircuitDigest 4語とimmutable circuitDigest0..3の比較（558-561）のみで、circuitConfigDigestはimmutable（78）を430でtranscriptに吸収する。`canonicalDigestPin c₀ c`（両digestが配備値に等しい）を`deploymentValid`に据え付け（proof側の直接比較はEngineに13番目のフィールドが要るためshape経由で推移的に固定）、受理⇒`c = c₀ ∨ khash衝突`（全19フィールド。15フィールドは衝突選言つき、WHIRのid 2つとdigest 2つは選言なし）、transcriptのframe 2/7が配備digestを吸収、`no_free_config_field_remains`。`DeployedFacts`（envelope・有界性・pinned digest・正準profile・decodeされたgate数）は全て配備側の事実で可住。`_validateConfiguration`（514-516）がdigest語の正準性を強制するため`Fin modulus`モデルはSolidityの受理集合を下から近似（安全側）。verifyCall読みでは本pinは配備不変条件の一部（immutableはcalldataに無い）。Rustはproof.circuit_digest==vk.circuit_digestとcircuit_config_digestの再計算でより強い。Solidity限定の配備意味論・R1b/R2・half (B)は不変 |
| [ExtractorConstruction](Audit/Wire3/ExtractorConstruction.lean) | 抽出接合（R2）の構成的消化。抽出状態を受理proofのused claims（cell 3=gatePreprocessed、cell 4=gateWitness）とR1b抽出器の出力から定義し（eq列は導出tauのeq table、rounds/pointは空の初期状態）、`GateClaimChain.TruthChain`がproverのmessageに言及しない定義的述語であることを確認して任意の整合状態に対し構成（message≠truthは`roundBadSet`が運用claimの非対角で吸収、対角では設計上空）。SoundnessAssemblyの残余19フィールドのうち10を消化：Consistent・TruthChain・cellsMatchClaims・eqProvenance・slotCoefficients・eqCellBinding構造は構成により、foldLevelOpening・extractedColumnHeight・committedWidth（`HonestOpenings.opened`がmapなので長さ一致＝fold水準述語の下でのみ既約）・lastCellsはR1bから。`fitColumn`は`hop.full`の下で恒等（`hop`無しでは埋め/切り詰めで何も同定しない）。`explicit_good_draw_of_extraction`の仮定は受理＋R1b（TailExtractsCommittedTables→この実行のHonestOpenings）＋TableIsCanonicalと行＋配備digestと有界性＋gate復号＋制約数正＋active filterのみで、gateTruths/sLast/xgは構成の出力（存在量化）。R1bはWHIR近接性＋sumcheck健全性（確率的・未形式化）を措定し、受理との同時充足可能性は未提示。結論は抽出表上のgood draw条件付き制約消失であり回路真理ではない |
| [RustCallBoundary](Audit/Wire3/RustCallBoundary.lean) | 「Solidity限定の配備意味論」を閉じる。Rustの`mle_verify_v2`（verifier_v2.rs 57-193）がcallごとに走らせる検査を行単位で対応付け（envelope/shape、採用済みrustAdmission・checkSubgroupGenPowers・validateConfiguration（verifier_v2.rs 422→gate_ext3.rs 539経由）、新規のkIs正準連鎖（g=14293326489335486720。CommonCircuitData未モデルのためbuilder由来の値を代用＝ソースより厳しい下近似）、circuit_config_digestの再計算（vk_v2.rs 198-293の前像を鏡写し、14メンバーで単射）、WHIR id）、`rustDeployment`（c₀を取らない：RustではVKが配備そのもの）と`rustEngine`を定義。(a) Solidity側受理＋配備事実⇒Rust検査成立∨khash衝突（3連言は受理のみで成立）、(b) Rust検査だけではSolidityのpinは出ない（whirEncodingはRustに入力が無く、circuitDigestも再計算すれば通る＝VKの由来が配備）、(c) rustEngine受理⇒構造的妥当性、(d) 配備config上で両engineは同じExcept値。**注意**: rustEngineもVerifier.verifyの骨格経由でconfigurationHash/chainId guardとpinned preprocessed rootを読む（(b)は述語についての主張）。残余: vk次元 vs common_data、wire-map範囲、体の位数/D、lookupは仮定。手動の鏡写しでありrefinementではない |
| [LocalizedCollisions](Audit/Wire3/LocalizedCollisions.lean) | 横断的所見の形式化。`CommitmentOrder.TranscriptCollision`・`ExplicitEngine.KhashCollision`・`RustCallBoundary.Khash2Collision`（∃ a b, a ≠ b ∧ hash a = hash b）は無限の入力域から有限のdigestへの任意の関数で成り立つ鳩の巣のトートロジーであり、`WhirRowBinding.RowCollision`の第2選言（長さ64の2入力の衝突）も有限の鳩の巣で常に真。よって採用済みの「P ∨ …Collision」形の定理（TC/KC/K2Cで37件・10モジュール、RowCollision/HashCollision系で17件）は文としては仮定なしに真で、監査内容は構成的証明の中の具体的な衝突入力にしかない（各件を`_statement_is_trivial`として明示）。**健全性欠陥ではないが文の形での過大主張**。さらに`ConditionalSoundness.no_row_collision_binds_opened_dot`は仮定`¬RowCollision`が充足不能で空虚（採用木側の要修正）。反証可能な局所述語（FrameFoldCollision＝折り畳みの第k frameでの衝突、Index/RoundDigestClash、ConfigEncodingCollision khash c c₀、LeafCollision）を定義し、主要定理（absorb_messages_bind_or_collide、used claims／round messageの非可鍛性、config digest binding、accepted_configuration_is_the_deployment、Rust境界(a)）を「…∨ 局所衝突」で言い直した（仮定追加なし、局所⇒大域、大域形を回復）。ConditionalSoundness 685-692が既にHashCollisionの鳩の巣を注記していたが同じfileが728で¬RowCollisionを仮定している |
| [RandomOracleSqueezes](Audit/Wire3/RandomOracleSqueezes.lean) | Fiat–Shamir半分(B)の最初の形式化。hashを「有限集合Q上の一様ランダム表」（random oracleの有限計数法則）としてモデル化し、相異なる入力での射影が一様であること（pushforward_uniform）、block digestを固定した条件付きでスケジュールのsqueezeがJointSpace上で一様でbad drawの質量≤combinedBoundであること（draw_at_fixed_digests_is_uniform／bad_draw_at_fixed_digests_le、d≤13でスケジュール仮定の充足を証明し、d=13の閉じた実例つき）を示す。block digest自体がframe問い合わせの出力である（各roundのframe入力は前のdigestを含む逐次依存）ことを見出し、frame入力とchallenge入力がprefixの15バイト目で分離されることから「block digestはchallenge形の回答に依存しない」congruence補題を証明。これによりframe fibre上のFubiniでrun水準の上界 P[bad draw] ≤ combinedBound + P[スケジュール非単射]（＝round digestの局所的衝突RoundDigestClash）を得た。**正直な読み**: 最小の閉包証人（frame入力を含まないQ）では加算項が恒等的に1で上界は空虚、実行のframe入力を含むQでは<1になる（存在を証明）が小さい上界は未証明（birthday型の計数と適応的proverが残る）。初稿の非適応定理はhfixが全digestをzeroDigestに強制し空虚だったため棄却・再定式化。大域衝突述語TranscriptCollisionは鳩の巣のトートロジー（LocalizedCollisions参照）。ROMであってkeccakの性質ではなく、WHIR/Merkle項を含まず系の健全性誤差ではない |
| [OpenedDotBinding](Audit/Wire3/OpenedDotBinding.lean) | 空虚な採用済み定理`ConditionalSoundness.no_row_collision_binds_opened_dot`（723行。仮定¬RowCollisionは充足不能）の修復。棚卸し：Lean上の利用箇所は0件（宣言と説明文のみ）で健全性への帰結はない。置き換え`opened_dot_binds_under_no_collision_among`は、実行から計算される有限リスト`openedDotInputs`（両openingの葉row bytesとMerkle親入力）上の単射性`NoCollisionAmong`（`OpeningBinding.NoCollision`と定義的に同値）を仮定し同じ結論を導く（採用済みaccepted_group_dot_binds_under_no_collisionの1回適用。仮定は証明が出しうる衝突対を全て排除し、多index openingでは同じ2実行の他のrow/pathも含む）。葉衝突に局所化した選言形、完全に具体化した実例（同一opening＝退化、非同一の変種も）、反証例（同root同indexでdotが異なる2つの受理opening）を備える。採用木側の推奨：723行の説明を非推奨化して本moduleを参照 |
| [BirthdayClashBound](Audit/Wire3/BirthdayClashBound.lean) | RandomOracleSqueezesのrun水準上界に残る加算項（round digestの衝突確率）のbirthday型計数。fresh query補題（qでの回答に依存しない事象Aに対しP(A∧T q=b)=P(A)/|Block|、`Function.update`のfibre全単射で等式として）と、問い合わせも標的も表に依存してよいadaptive版（回答に依存しないことを条件に、対合による全単射）、因果的で新鮮なchainに対するP[衝突]≤(n·|avoid|+n(n−1)/2)/|Block|。有界長の問い合わせ集合`boundedQueries L`（閉包条件を満たし、frame入力も含むのでROSの「加算項＝1」の状況ではない）上で、round foldの5 frameを1 roundとするchainモデルの衝突確率≤5d(5d+1)/2/|Block|（d=13で2145/|Block|<1）を証明。freshnessはframe_injectiveから証明（仮定ではない）、定数表は衝突事象に属し空事象への上界ではない。**正直な差分**: 関係prefixの22 frame（CommitmentOrder.gateChallengeFrames）を繋いだchainの起点が`derive`のdigestと一致することまで証明し（prefix_state_is_derive_digest）、87段のchainで衝突確率≤3828/|Block|<1を得たが、実行の`actualChain`への接続（concreteChainの帰納）は未了で、run水準の加算項epsは仮定のまま（2145/|Block|や3828/|Block|を実行の値として引用してはならない）。局所化定理はstageQueriesの同位置frame対を名指す文に強化。ROMであってkeccakではなく、固定prover、index laneは未対象、系の健全性誤差ではない |
| [ConcreteChainThreading](Audit/Wire3/ConcreteChainThreading.lean) | BirthdayClashBoundのchainモデルを実行の実際のcommit chainに同定する。`chain_model_is_the_run_chain`（round数r以下の各rについて、chainの22+5r段目のdigest = 採用済み`InstalledRoundCommit.concreteFinal`の先頭rメッセージ後のdigest。`concrete_final_take_succ`が採用済みモジュールが残したリスト帳簿）、`run_round_digest_is_chain_digest`（採用済み`sourceDigest`はchainの22+5r+5段目）、事象の**等式**`clashEvent = prefixRoundDigestClashEvent`、よってRandomOracleSqueezesのrun水準上界の加算項を放棄: `run_bad_draw_probability_le_birthday`は固定（非適応）プローバのROM下で確率的側条件を残さないrun水準上界（≤ combinedBound + (22+5d)(22+5d+1)/2/|Block|、d=13で3828/|Block| ≤ 2^-244）。`LengthBudget`（7条件）の閉じた証人（L=381, testConfig at degreeBits 13）、閉じた実例`run_bad_draw_probability_le_birthday_closed`とそのRHS<1（`closed_bound_lt_one`）、非空性（定数表∈clash事象、boundedQueriesはframe-freeでない）。ROMでありkeccakではない、プローバ固定、外側sumcheck laneのみ、系の健全性誤差ではない。 | 32 |
| [IndexLanesOracle](Audit/Wire3/IndexLanesOracle.lean) | index laneをROM法則下に置く。`prefixRoundIndexShape`（22 prefix + 5d round + 採用済み`claimFrames`の8 frame）と`index_state_is_index_digest`（22+5d+8段目 = 採用済み`InstalledIndexSampler.indexDigest`。absorbはcounterを読まないので`index_digest_congr`）、橋渡し補題（`frame_state_agree_below`/`extended_prefix_agrees`/`extended_stage22_is_derive`/`extended_round_stage_five`）で拡張chainの22段目と22+5i+5段目を採用済みderive/roundCommitted digestに同定。固定index digest下で`actualIndexDraw`の押し出しは採用済み`indexProbability`に**厳密に**等しく（3 squeeze/座標、2·bits座標。mod pの偏りは採用済みtauTerm側）、guard付きindex bad事象は≤2·tauTerm。索引カウンタは外側カウンタと数値的に重なる（`index_counters_meet_round_counters` rfl）ので分離はdigestのみ、`joint_and_index_bad_at_fixed_digests_le`は**固定digest版**の和集合上界（≤ combinedBound + 2·tauTerm; 消費するのは2族それぞれの単射性のみ）。拡張chainのbirthday上界(22+5d+8)(22+5d+9)/2/|Block|（d=13で4560/|Block|<1）。充足性定理群と閉じた包絡実例。**未実施**: 和集合に対するrun水準Fubini。`hst`（22+5d段目=post-rounds snapshot）は本モジュールでは仮定（ConcreteChainThreadingが同じ同定を証明するが、Lean上の接続は未了）。ROMでありkeccakではない、プローバ固定、系の健全性誤差ではない。 | 67 |
| [RunLevelUnionBound](Audit/Wire3/RunLevelUnionBound.lean) | 一つのROM法則下でのrun水準の和集合上界。IndexLanesOracleの`hst`を放棄（拡張chainの22+5d+8段目 = `SoundnessAssembly.actualIndexDraw`が絞るrunの索引digest。`assembly_index_draw_is_at_run_index_digest`はrfl）、RandomOracleSqueezesのframe fibre Fubiniを**2族同時に1回**再実行（`frame_state_frame_congr`でchain全段がframe半分のみの関数、clash外のfibreで結合scheduleが単射、fibre計数は`restrict_ratio`で厳密）: `run_union_bad_draw_probability_le` P[jointBad ∪ guardedIndexBad] ≤ combinedBound + 2·tauTerm + P[extendedDigestClashEvent]、birthday形で d=13 に 4560/|Block|、閉じた実例（L=381、testConfig at degreeBits 13 / indexBits 8）と`run_union_closed_bound_lt_one`、good tableの存在。`assembly_failure_subset_run_union`: explicit engineの組立結論が失敗し得る表の集合はhash添字付き`runUnionBadEventAt`に含まれる（無条件の包含）。**質量上界は未達**: 素朴な被覆仮定（∀hashで固定事象が支配）は非退化な実行で充足が知られないため、レビュー後に被覆定理と質量系を削除。残る橋渡しは固定プローバの2段階条件付き計数（alpha条件付きの外側対角事象 ≤ combinedBound、外側draw条件付きで索引drawが一様 ≤ 2·tauTerm）であり適応性の問題ではない。`hlenI`はLengthBudgetに加えp.usedの5つの幅上界と69+24W ≤ Lを要し、L=381は幅≤13のみ（配備のindexBits=8はILOのL=8192）。閉証人の索引半分は∅。ROMでありkeccakではない、固定プローバ、系の健全性誤差ではない、R1bは残余内。 | 43 |
| [StrategyChainBound](Audit/Wire3/StrategyChainBound.lean) | 適応的プローバへの第一歩: fresh-query帰納をリストではなく**戦略**に対して再実行（chainのclash事象のみ）。`Strategy`は履歴（既出の段digestと、それらのdigestのchallenge入力に対する神託の回答）から次frameのtag/payloadへの関数で、因果性は型に組込み（`strat_hist_eq`）。固定shapeのchainは戦略の特例（`strategy_chain_of_fixed_shape`）。任意の戦略に対して`strategy_chain_clash_probability_le ≤ n(n+1)/2/|Block|`（87段で3828/|Block|、固定プローバと同じ定数。新素材は`strategy_sel_stable_gen`と採用済み`frame_ne_challenge_input`: frame回答の書換えはchallenge回答を動かさない）。fresh-uniform challenge補題（`strategy_challenge_fresh_uniform`、有限カウンタ集合版`strategy_challenge_joint_uniform` = P[noClash]/|Block|^|C|、`challenge_input_digest_eq`）: chain自身のno-clash事象で条件付けても、段jのdigestのchallenge回答は一様。戦略的外側プローバ（`strategicShape`, `RoundCausal`, `realizedMessages`）のchainは表ごとに実現メッセージの固定shape chainに一致し、`concreteFinal`まで通る。戦略は**transcript制限付き**（自前の神託問合せを持たない）: 候補payloadを自分でhashして衝突を探すROMのgrindingプローバ（衝突質量 ~q·n/|Block|）は対象外と明記。**未達**: 適応的プローバの和集合上界（combinedBound側）。OuterSequentialConditioningの理想モデル内の逐次条件付け定理を神託表へ輸送する段（入れ子のno-clash事象を13 round digestにわたって連鎖）が残る。非空性: 定数表は全戦略のclash事象に属し、challenge回答を実際に読む戦略`firstChallengeStrategy`を提示。ROMでありkeccakではない、外側laneのみ、系の健全性誤差ではない。 | 53 |
| [GrindingQueryBound](Audit/Wire3/GrindingQueryBound.lean) | プローバ自身の問合せを数える: StrategyChainBoundのHONESTY (vii)が除外していたgrindingプローバ（各段でq回のprobe問合せを逐次適応的に行い、その回答を見てからabsorbを選ぶ）に対するchainの衝突上界。採用済み`BirthdayClashBound.Causal`は再問合せ（probeしたframeをそのままabsorb）を許すgrindingには適用不能（freshness節が偽、さらに同表ではstability節も偽）なので、freshness事象で条件付ける`FreshCausal`を導入し（`causal_of_fresh_causal`で採用済み概念を弱めていないことを記録）、fresh-query帰納を再実行。`distinct_answer_clash_probability_le`: 異なる文字列を問うた2位置の回答一致（またはavoid命中）の質量 ≤ (n|avoid| + n(n−1)/2)/|Block|（同一文字列の反復は第一出現論法で押し下げ、仮定しない）。probeとabsorbを合わせたN=(q+1)n位置の因果列で`grinding_chain_clash_probability_le ≤ N(N+1)/2/|Block|`（q=0で採用済み3828/|Block|、q=2^20でも ≤ 2^-200 を記号的に）。q=0はSCBの戦略chainと表ごとに一致。probeの回答を実際に読む`probeGrinder`はtranscript制限付きの任意の戦略と（ある表で）chainが異なる。probe（列の位置）はframe形の文字列に限定だが、challenge入力の読み取りは**任意のdigest**で無償に許す（`challengesOf`はdigest索引。frame回答の書換えはchallenge回答を動かさない）。上界はqについて2次（全対上界）で、先頭項は≈(q+1)倍緩い（SCBのHONESTY (vii)の「q·n」は発見的表現）。chainの衝突事象のみ。適応的/grindingプローバのjointBadEvent上界は未達。ROMでありkeccakではない、外側laneのみ、系の健全性誤差ではない。 | 65 |
| [TwoStageConditionalCount](Audit/Wire3/TwoStageConditionalCount.lean) | RunLevelUnionBoundの契約（固定プローバの2段階条件付き計数）の外側段を完全に放棄。採用済み`outerBadEvent`はhashにgate alpha座標経由でのみ依存し（`outer_bad_event_depends_on_alpha_only`）、explicit engine自身のhash依存も吸収（lane message/truthは証明から読む、`frozenLane`はchallengeを読まない）。gate alphaは外側drawの1座標（`alpha_is_the_alpha_coordinate`、無条件）なので正しい対象は**対角事象**（各点が自分のalpha座標でのbad集合に属する）で、`stage_a_mass_le`はそれを採用済み`combinedBound`で（項ごとに同じ定数で）評価: 一座標の条件付き計数`diagonal_mass_le`（各F aがalpha座標から独立な柱であることが実効仮定。`invariant_fiber_card`）、alpha項は非対角化のまま。輸送`run_outer_stage_mass_le`（対角は固定Finsetなので RLUB のfibre Fubiniがそのまま適用）。Stage B（索引段）: 依存事実は両方証明（供給cellは索引drawから独立、committed cellはrow点のみ経由、そして**row点は外側drawの関数**`row_point_determined_by_outer_draw` — 初稿の「log-tau列を読むので違う」はレビューで反証され撤回）。索引段の条件付き計数そのものは未達で、`RowPointFixed`（committed cell族が全hashで一定）を明示仮定として払い出し（証人: 空列、および定数列 replicate(2^rounds) v で非退化）、`assembly_failure_mass_le`（≤ combinedBound + 2·tauTerm + P[拡張clash]、d=13で+4560/|Block|）は**定数のcommitted列のみ**を覆う。閉実例（旧証人は索引半分が∅と定理で記録、`liveProof`の証人ではguardが生きる）とRHS<1、good table。抽象の2群条件付き計数`product_diagonal_card_le`と細分割の3段契約を残す。ROMでありkeccakではない、固定プローバ、系の健全性誤差ではない、R1bは残余、受理は未提示。 | 78 |
| [IndexStageFinerSplit](Audit/Wire3/IndexStageFinerSplit.lean) | TwoStageConditionalCountのHONESTY (6)が残した3段契約を全て構築し、`RowPointFixed`仮定を**計数で除去**する（述語を証明するのではない。非定数列では偽）。`frame_fibre_card`（表事象とfibre族を抽象化したframe fibreの剥離）、`run_index_diagonal_fibre_count_le`（RLUBの結合schedule `Sum.elim selJ selI` はclash外で単射なので外側cell K1と索引cell K2は互いに素、採用済み`restrict_ratio`で(外側draw, 索引draw)がK1⊕K2上で同時一様、TSCCの`product_diagonal_card_le`で群ごとに計数）、`run_union_diagonal_probability_le_fibrewise`（索引半分がrun自身の外側drawに依存してもよい、加算項は1つ）。索引段: `rowPointOf`/`cell_row_is_row_point_of`でengineのrow点をjoint空間の点から読み、`index_bad_event_at_is_index_bad_at`でhash添字付き索引族を外側drawでの`indexBadAt`に変換、各要素は採用済み`guarded_index_bad_event_mass_le`で≤2·tauTerm。見出し`run_index_stage_mass_le ≤ 2·tauTerm + P[拡張clash]`（仮定はhL/hdb/hbb/hlenI/hlenP/hmlenのみ）。payoff `assembly_failure_mass_le_unconditional`: TSCCの仮定から`RowPointFixed`を除いたもの、committed列`cols`は任意、RHSはcombinedBound + 2·tauTerm + P[拡張clash]（d=13で+4560/|Block|）。閉実例（TSCCの`liveProof`、任意cols）でRHS<1とgood tableの存在、定数列では対角の全点でguardが生きる。未解決: 非定数committed列でのguardの生存性（guardが死んでも上界は偽にならず2·tauTermが遊ぶだけ）。ROMでありkeccakではない、固定プローバ（適応性は一切広がらない）、外側+索引laneのみ、系の健全性誤差ではない、R1bは残余、受理は未提示、L=381は幅≤13。 | 23 |
| [OuterLaneTransport](Audit/Wire3/OuterLaneTransport.lean) | OuterSequentialConditioningのprefix依存な各round条件付けを、戦略駆動chainに沿って神託表へ輸送する。技術的核: 対角fresh step（`stage_triple_target_card`: 条件付け事象を**targetの値で分割**し各ブロックでtargetが定数、3カウンタの逐次剥離で `|{T∈A | triple T ∈ targ T}|·|DigestTriple| ≤ m·|A|`。採用済み`adaptive_fresh_step`は表依存targetを既に許しており、laneのround bad集合がその仮定を満たすことを証明）、`outer_state_reassign`（輸送されたPrefixDependent）、入れ子のno-clash事象による和（`stage_no_clash_mono`）、厳密な採用済み定数`outerTerm`（`round_terms_sum`）。一般の見出し`outer_lane_bad_draw_probability_le ≤ outerTerm d q + (22+5d)(22+5d+1)/2/|Block|`は**戦略とlaneを独立に量化**しており（判定 iii: laneのメッセージは縮約challenge prefixの関数で戦略には結びつかない。`laneOfStrategy`は一般の`Strategy`に対して構成不能: 定義域〈raw blockを全段全カウンタで読む〉と損失〈reduceTripleは非単射〉。SCBの`challengeTruncatedMessage`もstage 22をfromLeで読むため該当）、SCB HONESTY (iii) を一般には閉じない。§8の**縮約履歴部分クラス**（`ReducedStrategy`: 縮約round challenge履歴のみの関数。`strategyOfReduced`で埋め込み、`logLaneOfReduced`/`gateLaneOfReduced`との対応`log_lane_of_reduced_message_is_realized`を証明）では`reduced_outer_lane_bad_draw_probability_le`が**真の適応的和集合上界**（roundのbad集合はプローバが選んだメッセージで決まる）となり、この部分クラスに限りSCB (iii)を閉じる。非空性は両方向を定理化（閉実例のbad集合はround 0/1で∅、`separatedLane`ではround 0で非空）。ROMでありkeccakではない、transcript制限付き（grinding除外）、外側laneのみ、定数はouterTermのみ（tauTerm/alphaTermは未輸送）、系の健全性誤差ではない。 | 92 |
| [IndexGuardLiveness](Audit/Wire3/IndexGuardLiveness.lean) | IndexStageFinerSplitのHONESTY (9)（非定数committed列でguardが生きるか）を閉じる説明モジュール。上界は一切変えない。採用済みguard `CellsDifferOnCube bits supplied committed` は「∃ j < 2^bits, supplied.getD j 0 ≠ committed.getD j 0」（`cells_differ_iff`）で、共通幅 ≤ 2^bits では dead ↔ supplied = committed（`guard_dead_iff_equal`。一般の消費者は幅の一致を仮定しないので dead 条件は0詰め一致 `guard_dead_iff_cube_agreement`。1cell形 `cells_differ_singleton_iff` は閉証人の形）。deadなguard = 主張cellがcommitted列の拡張と一致する**正直な場合**であり、そのときguard付き索引事象は∅（`dead_guard_kills_index_event`）。非定数列の代表 `spikeColumn m v w`（頂点0だけw、他はv）の多重線形拡張は `v + (w−v)·eqAtZero tau`（`extension_spikeColumn`、eqAtZero = Π(1−tau_i)）なので、guardは `eqAtZero row = (x−v)/(w−v)` のときに限り死ぬ（`guard_dead_at_nonconstant_column_iff_ratio`）。特にx∉{v,w}でも死に得ることを行 [1−x] で示す。dead な外側drawの集合は準位集合として記述（Schwartz–Zippel型の質量上界は未着手）。閉実例（`liveProof x` vs `spikeColumn m 0 1`）: 全零行では ⟨x⟩≠1 で生存、1を含む行では ⟨x⟩=0 で死亡。提示した行点が実際の外側drawで実現されることは示していない。 | 35 |
| [GrindingLinearBound](Audit/Wire3/GrindingLinearBound.lean) | GrindingQueryBoundの全対上界 N(N+1)/2（N=(q+1)n、qについて2次）をqについて**線形**にする: `grinding_chain_clash_probability_le_linear ≤ (q+1)·n(n+1)/2/|Block|`。GQBが名指した障害（位置の役割に依存する側条件は反復の押し下げで保存されない）を、押し下げを問合せ**文字列**上で言い直し（`exists_first_occurrence`）、位置依存target `stateTarget m T`（位置mまでに形成済みの段digestのblock、≤ m/(q+1)+1 ≤ n+1、mより前の回答のみに依存 = 採用済みFreshStepの表依存target）で解決。三角計数 `two_mul_sum_slot_card`、包含 `grinding_clash_state_framed_subset`（後段についての強帰納法）。**制限**: `StateFramedProbes g`（probeは既に形成済みの段digestでframeする。absorbは構成上満たす）。除外される攻撃者: probe回答のdigestをabsorbせずに更にprobeし、probe予算内で候補chainを模擬してから勝ち筋をabsorbで再生するプローバ（2つの衝突回答が共にprobeになり得る。問合せグラフ上の計数が必要で未着手。GQBの2次上界は彼も覆う）。q=0では制限なしで採用済み定数と一致（`grinding_chain_clash_at_eighty_seven_linear_no_probes` = 3828/|Block|）、任意のq,nで線形定数 ≤ GQBの定数、発見的 q·n/|Block| よりは ≈n/2 倍大きい。任意戦略向けの残差形（未評価の off-state-frame 項つき、空虚になり得ると開示）。n=87, q=2^20 で 4013952756/|Block| ≤ 2^-220。probeGrinderはstate-framed。chainの衝突事象のみ。ROMでありkeccakではない、外側laneのみ、系の健全性誤差ではない。 | 35 |
| [ReducedFullTransport](Audit/Wire3/ReducedFullTransport.lean) | OuterLaneTransportの縮約履歴部分クラス上界（定数はouterTermのみ）を、derive digest（戦略chainの22段目）でのblock-0 squeeze — gate tau と gate alpha — まで拡張し、定数を**完全なcombinedBound**にする: `reduced_full_bad_draw_probability_le ≤ combinedBound d q d constraints + (22+5d)(22+5d+1)/2/|Block|`（`fullBadEvent = outerLane ∪ gateTau ∪ gateAlpha`、衝突項は入れ子no-clashにより共有）。採用済み`tauBadEvent d g`/`alphaBadEvent d rows coeffsOf`はround message・lane・戦略・表のいずれにも依存しない**固定データ**（g, coeffsOf）なので、OLTの固定targetの質量補題で足りる（対角は不要）。tau事象は座標積でないため3dカウンタ分を同時に剥離（`tauFibre`/`tau_fibre_card`: 厳密計数 |fibre|·|DigestTriple|^d = |noClash 22|）。任意のStrategy/Lane対の一般形`full_bad_draw_probability_le`、閉実例`reduced_full_bound_at_thirteen ≤ combinedBound 13 8 13 123 + 3828/|Block|`とRHS<1。**含まないもの**: 索引lane（使用claimはroundの後に吸収され、適応的プローバでは表の関数。固定target補題は使えず対角形が必要。ISFSの2·tauTermはこのモデルでは未証明と明記）。g/coeffsOfが配備された表であることは主張しない（CommitmentOrderの結合は未了のまま）。22段目 = derive digest と両事象の引き戻しは定理（`derive_stage_is_derive_digest` ほか）。commitment rootは文の側で chain の前に固定（だからg/coeffsOfは固定データ、同時にこのプローバはcommitmentを適応的に選べない）。縮約履歴部分クラスのみ（raw block読みの戦略とgrindingは除外）、ROMでありkeccakではない、系の健全性誤差ではない。 | 37 |
| [ReducedIndexLanes](Audit/Wire3/ReducedIndexLanes.lean) | ReducedFullTransportが除外していた索引laneを、縮約履歴適応プローバに対して取り込む。プローバは全round後に**履歴依存の used-claims 選択** `U : List Element → UsedClaims`（読めるのは全roundの縮約challenge 2d個のみ。raw block・カウンタ>5・非round段・prefix digest・索引digestの回答は読めない）を行い、それが8つのclaim frameとして吸収される（`indexStage d = 22+5d+8`。`extended_stage_is_index_digest`: 22+5d+8段目 = 実現claimsでの採用済み`indexDigest`、`index_read_is_actual_index_draw`）。索引digestでの対角fresh step（索引カウンタは外側カウンタと数値的に重なるので分離はno-clash下のdigestによる。`realized_claims_reassign`: target〈U(履歴)での供給cellと committed(履歴)〉は索引cellの再割当で不変）、密度補題 `idx_diagonal_mass_le_density`（≤ 2·tauTerm bits）、6·bitsカウンタの同時剥離、入れ子no-clashで衝突項は1つ: `full_index_bad_draw_probability_le_combined ≤ combinedBound d q d constraints + 2·tauTerm bits + (22+5d+8)(22+5d+9)/2/|Block|`。閉実例（d=13, q=8, bits=8, previousChallengeMessage, 任意U）でRHS<1（2^-172 + 2^-185 + 2^-243）。**開示**: 供給cellはp.usedの射影として証明済みだが、row点での committed cell は履歴の関数としての**パラメータ**（列が固定データなら実際のcommitted cellは縮約履歴の関数なので∀形で覆われる。転送に必要な補題群は明示、未実施。RowPointFixedは不要）。索引項は1 bound cell分（5 cellなら5·2·tauTerm）。ROMでありkeccakではない、grinding/raw block戦略除外、WHIR/Merkle除外、g/coeffsOf/committed列は配備と未結合、系の健全性誤差ではない。 | 54 |
| [GrindingGraphBound](Audit/Wire3/GrindingGraphBound.lean) | grindingプローバの**問合せグラフ**による見方。結論は否定的で、それを定理として述べる: GrindingLinearBoundが除外したchain模擬プローバ（probe回答のdigestをabsorbせずに更にprobeし、候補chainをprobe予算内で模擬）に対して、グラフ精密化は健全だが定数はGQBの全対 N(N+1)/2 のまま改善しない。内容: (2) 任意戦略で `grinding_clash_subset_path`（段の衝突は**absorb経路上**の2位置の相異文字列衝突に帰着。`pathClashEvent ⊆ distinctClashEvent`）、`GraphEdge`/`ReachIn`（深さ上限と辺予算、単調）。(4) 統一charging定理 `grinding_clash_le_eligible_sum`（再割当不変な位置依存eligible集合 E m T と基数上界 c m から P[clash] ≤ Σ c m/|Block|）で、GQB（chainTarget, c m = 1+m, 仮定なし）、GLB（stateTarget, c m = m/(q+1)+1, StateFramedProbes）、新しい弱い `GraphFramed` 下の reachTarget を3つの実例として統一。(3) `chainSimulator q` を具体的に定義し、state-framedでない（`chain_simulator_not_state_framed`）がgraph-framedであり、その reachTarget は chainTarget に一致する（`chain_simulator_reach_target_eq`。フィルタは何も除かず charge は 1+m）ことを示して、charging方式では線形定数に届かないことを **charging集合についての定理**として記録（衝突質量の下界は主張しない）。数値はGQBと同じ（87段・q=2^20で 4161109737606900/|Block| ≤ 2^-200）。chainの衝突事象のみ、probeはframe形、ROMでありkeccakではない、系の健全性誤差ではない。 | 59 |
| [ReducedEngineIndex](Audit/Wire3/ReducedEngineIndex.lean) | ReducedIndexLanesのHONESTY (iii)が「未実施」とした転送を実施する: 縮約履歴適応プローバの索引上界を、explicit engine **自身の**索引bad事象（`SoundnessAssembly.indexBadEvent` を `actualIndexDraw` で評価したもの）の上で成立させる。材料: 二戦略・同一表の合同補題（`strategy_frame_state_of_agree` 等: extendedShapeとstrategyOfReducedは22+5d未満の段で一致するので段digestが一致）、OLT §6 の draw 同定を実現proofに適用（`run_log_draw_is_history_entry`/`_gate_`、`outerHistory` の偶奇帳簿）、TSCCの`row_point_is_outer_draw_function`を `realizedRun`（used := U(履歴) を持つ新しい実現proof）で実体化（`lift_cell_row_is_row_from_history`）、列は固定データという既存の仮定。結果 `committed_is_history_function` → `engine_index_bad_event_eq`（engineの索引bad事象とRILの事象の **Finset恒等式**）→ `reduced_engine_index_bad_draw_probability_le ≤ combinedBound + 2·tauTerm + (22+5d+8)(22+5d+9)/2/|Block|`。多cell版 `…_all_cells`（5 bound cellの和集合、5·(2·tauTerm)、衝突項は1つ。`explicit_good_draw_assembly`のhidxはcellごと）。副産物: RILの`hst`を`actualIndexDraw`が名指すconcreteFinal snapshotで放棄（CCTの`chain_model_is_the_run_chain`）。閉実例（d=13, q=8, bits=8）でRHS<1、`envelope_config_hypotheses`。非空性: 行の長さd、正直なcellではguardが噛んで∅。ROMでありkeccakではない、縮約履歴のtranscript制限付きプローバ（grinding/raw block除外）、列とg/coeffsOfは配備と未結合（CommitmentOrder結合は未了）、WHIR/Merkle除外、結論はgood-draw条件付き、R1bは残余、受理は未提示、系の健全性誤差ではない。 | 48 |
| [RawBlockLanes](Audit/Wire3/RawBlockLanes.lean) | OuterLaneTransportの reduced-history 制限を解除する。OLTの2つの障害（定義域・損失）は「raw メッセージをOSCの`Lane`の引数に因数分解する」ことへの障害であって、対角fresh stepへの障害ではない（round rのtargetがround r自身の回答で動かないこと = `RoundCausal`のみが必要で、raw か縮約かに無関係）。そこで`Lane`を捨て、`RawLane.message : Nat → (Nat → Nat → Block) → List Element`（rawの履歴を読む）と、検証器が実際に評価する**縮約challenge**での`OuterRound.evaluate`による running claim を持つraw laneを定義。`rawLogLaneOfStrategy`/`rawGateLaneOfStrategy`はOLTが「構成不能」とした`laneOfStrategy`（各1行）。`raw_outer_state_reassign`、`raw_target_reassign`（因果性のみから、段の全カウンタで）、`raw_target_card_le`、`raw_round_mass_le`。見出し `strategic_raw_outer_bad_draw_probability_le`: **任意の**RoundCausal S に対して ≤ outerTerm d q + (22+5d)(22+5d+1)/2/|Block|（結合 `raw_log_lane_message_is_realized`: laneのround rメッセージ = 戦略の実現メッセージ）— SCB HONESTY (iii) の**各round分解の半分**を全transcript制限付き因果的プローバについて閉じる（同項末尾のrun水準輸送は未着手）。`strategic_raw_full_bad_draw_probability_le ≤ combinedBound + chain衝突項`（RFTのtau/alphaは固定targetなので逐語的に転送、衝突項は共有）。OLT §8は特例（`raw_outer_bad_event_of_reduced`、Finset等式）。閉実例は**真にrawを読む**プローバ`challengeTruncatedMessage`（`truncated_gate_lane_is_not_reduced_history`: OLTが対応づけられなかった戦略）で ≤ combinedBound 13 8 13 123 + 3828/|Block| < 1。**未達**: 索引半分（RILがReducedStrategy上で述べられており約600行の機械的再導出が必要。2·tauTermは定数に含まれない）。ROMでありkeccakではない、自前問合せなし（grinding除外）、g/coeffsOf/列は固定データ、系の健全性誤差ではない。 | 60 |
| [RawIndexLanes](Audit/Wire3/RawIndexLanes.lean) | RawBlockLanesが残した索引半分をraw戦略に対して与え、ReducedEngineIndex型のengine転送まで行う。`rawExtendedShape c s S U`（22+5d未満はstrategicShape、以後はraw view（22+5d段まで）から選んだ`U`の8 claim frame）、`ClaimsCausal d U`（Uは22+5d段までのrawビューのみを読む = 全round challengeの後・索引squeezeの前という議定書の因果性）、`raw_extended_stage_is_index_digest`、`raw_index_read_is_actual_index_draw`。RILの計数スタック（idxFibre/idx_fibre_card/idx_diagonal_mass_le_density 等）とREIの転送補題（AgreeBelow, committedOf, guarded_is_assembly_index_bad_event 等）は戦略非依存で**逐語的に再利用**、新規は不変性補題2本（`raw_realized_claims_reassign`: RBLの`table_view_reassign_lower` + ClaimsCausal、`raw_history_reassign`）。engine転送はOLTの`strategic_run_log_draw_is_log_triple`がstrategicShapeに直接適用できるためREIより単純。見出し `raw_engine_index_bad_draw_probability_le`: **任意の**RoundCausal SとClaimsCausal Uに対し、engine自身の`actualIndexDraw`が自身の`indexBadEvent`（`rawRealizedRun`で評価）に落ちる事象とRBLの全事象の和集合の質量 ≤ combinedBound + 2·tauTerm + (22+5d+8)(22+5d+9)/2/|Block|（衝突項1つ）。5 cell版は5·(2·tauTerm)。閉実例はS := challengeTruncatedMessage（真にraw）、U := blockClaims（stage 22のraw blockを読む。`block_claims_depends`: 縮約履歴では区別できない2つのビューで異なるclaims）でRHS<1、StrategyBounded/64≤L/カウンタ予算は残らない。非空性: 正直なcellでは∅、行長d。ROMでありkeccakではない、自前問合せなし（grinding除外）、列/g/coeffsOfは固定データ（CommitmentOrder結合未了）、WHIR/Merkle除外、結論はgood-draw条件付き、R1b残余、受理未提示、SCB (iii)末尾のrun水準輸送は別途、系の健全性誤差ではない。 | 50 |
| [RunLevelTransportAudit](Audit/Wire3/RunLevelTransportAudit.lean) | StrategyChainBound HONESTY (iii) 末尾の「whole-schedule形、したがって適応的run水準上界は未証明」を決着する。採用済み`jointBadEvent`の外側部は凍結された`LaneRound`リスト（message, truth, challenge）を取り、claimの再帰は各LaneRound**自身の**challenge欄で進み、`roundBadSet`はchallengeを読まない。よってchallenge欄に実現された縮約tripleを入れるとRBLの`rawClaims`と定義的に一致し、`raw_full_bad_event_is_run_level_event`（Finset等式）が成立。`realized_draw_is_actual_digest_draw`（座標ごとの輸送: 戦略chainの段tripleは実現proof `realizedRunProof T` における採用済み`actualDigestDraw`。CCTの`run_round_digest_is_chain_digest`を表依存proofで適用、SCBのchain同定、derive digest）。結論 `adaptive_run_level_bad_draw_probability_le`: 任意のRoundCausal Sに対し P[T | actualDigestDraw(hashOf T) c (realizedRunProof T) ∈ jointBadEvent(…実現lane…)] ≤ combinedBound + (22+5d)(22+5d+1)/2/|Block| — 固定プローバのCCT `run_bad_draw_probability_le_birthday` と**同じ文の形**でproofとlaneを実現版に置き換えたもの。閉実例（challengeTruncatedMessage、hd : degreeBits = 13）でRHS<1。Q2: whole-schedule一様性は**不要**（`run_level_event_is_a_union_of_one_stage_events`: 事象は各段の一段事象の和で、union boundは段ごとの質量しか消費しない）。正確な積法則については、no-clash事象の内側段の再割当に対する閉性を与える採用済み補題が無いためStageStable経路は開いておらず、成否はここでは未決（反例表も構成せず）と散文で記録。Q3: SCB (iii)/OLT (vi)/RBL (vi) の修正文を提案（本batchで採用済みdocstringに反映）。`actualDigestDraw`に法則を付けるものではなく、表法則の下で事象の質量を評価するだけ。ROMでありkeccakではない、transcript制限付き、外側+block-0（索引はRawIndexLanes）、系の健全性誤差ではない。 | 44 |
| [CommitmentOrderSurvey](Audit/Wire3/CommitmentOrderSurvey.lean) | ROM系列が繰り返し「g/coeffsOf/committed列は固定データ、CommittedTables結合は未了」と述べる残余の正体を分類する調査モジュール。五分類: (a) 配備データ、(b) prefixに吸収されたproof材料（公開入力・root）、(c) R1b抽出（`InstalledWhirTail.TailExtractsCommittedTables`）、(d) 真の未結合（`cellsMatchClaims`、`CommitmentOrder.CommittedTables`。`opening_relation_ignores_the_commitment`が示すようにモデルはrootと列を結びつけない）、そして **(e) 同じderive digestの先行カウンタで squeeze された challenge**。決定的な二点: (1) assemblyのtau表 g = `gateValue … (gateAlphaElement …) tables` は block-0 の **gate alpha**（counter 9+3dのsqueeze = challenge座標 `Draw.gateAlpha`）を読む（`assembly_tau_table_factors_through_the_prefix_alpha`）ので、ROM系列の固定gのtau事象（RFT以降）は engine 自身の tau 事象の **alpha固定スライス**であり、engine自身のtau事象（alpha三つ組上の対角）は同batchのEngineTauDiagonalが評価する（alphaカウンタは全tauカウンタと素: `gate_alpha_counter_is_outside_every_gate_tau_triple`）。coeffsOfは表のみの関数（challengeを読まない）。(2) committed列は **prefixデータではない**: 抽出器は(roots, round-one opening)の関数で、openingは索引squeeze（22+5d+8）の**後**にWHIR tailで吸収される（`absorption_order_of_the_extractor_inputs`）。`extracted_state_moves_with_the_opening`は同じrootsで異なるopeningから異なる抽出状態を構成的に示す（0 < numWires）。従って「列は固定データ」= `CommittedTablesJoin.rootDetermined`（R1b型の仮定。列はrootsの関数）であり、g/coeffsOfとは異なる地位。`CommittedTablesJoin` = rootDetermined ∧ preprocessedPinned ∧ configDeployed（退化した定数抽出器で充足可能）とその帰結。REPORT §5に採用済み6ファイル12箇所の修正文（本batchで適用）。 | 29 |
| [GrindingUnionBound](Audit/Wire3/GrindingUnionBound.lean) | grinding chain（各段q回のframe形probe、GQBのモデル）に沿った**和集合上界**（外側+block-0）: `grinding_union_bad_draw_probability_le_combined ≤ combinedBound d quo d constraints + N(N+1)/2/|Block|`（N=(22+5d)(q+1)、GQBの全対定数、qが入るのはここだけ）。前提 `ChallengeRestricted g`: 段kのprobe/absorb選択はchallenge関数を**自身の段digest 0..k**でのみ読む（採用済みStrategyに渡される情報と同じ。持ち上げた全Strategyが満たす `grinding_of_strategy_challenge_restricted`、probeGrinderも満たす — より広いクラス。q=0でのRBL事象との一致は未証明）。採用済みOLT/RFTの剥離はSCBの`challengeSel`で述べられており grinding chain は戦略chainではないので、抽象的な段digest族上で計数を言い直した（`ChainStable`、`chain_triple_target_card`）。lane は `GrindLane`（**probe回答履歴 + challengeビュー**を読む。`GrindCausal q`は採用済みRawCausal節 + 位置 < (22+5r+1)(q+1) のrun側節。roundのchallenge段より厳密に下の cut で、5 frameの間に probe を挟むプローバは未モデル）で、対角stepは`grind_run_view_reassign_lower`（probe回答はframe cellなのでchallenge cellの再割当で不変）。採用済みraw laneは`grindLaneOfRawLane`で持ち上がる。**除外される攻撃（HONESTY）**: probeの回答digestでchallengeを読んでからメッセージを選ぶ**pre-read**プローバ。orchestratorの「拡張clash事象で条件付け」案は反証済み — probe→absorbは**同一文字列**なのでGQBの相異文字列衝突では charge できず、`echo_grinder_pre_read_mass_one`（pre-read事象は全空間、質量1）。`preReadGrinder`（selector: 読んだchallengeが目標なら probe 0 の payload を absorb。`pre_read_grinder_absorb_is_probe_when_favourable`で chain が pre-read digest へ移ることを証明）は ChallengeRestricted ではない。これはFiat–Shamir の challenge grinding そのもので、birthday項では評価できず問合せ計数が必要。**未達**: 結合 `grindLaneOfGrinder`（bad集合が grinder 自身の absorb payload を運ぶ形。bytes→elements復号が必要）— 現状「laneはgrinderが読んだものを読める」まで。閉実例（probeGrinder, q=2^20, separatedGrindLane）で ≤ combinedBound 13 8 13 123 + 4161109737606900/|Block| < 1（算術の例示であり適応的シナリオの証拠ではない）。ROMでありkeccakではない、外側+block-0のみ（索引lane別）、系の健全性誤差ではない。 | 74 |
| [EngineTauDiagonal](Audit/Wire3/EngineTauDiagonal.lean) | CommitmentOrderSurveyのレビューが露わにした隙間を閉じる: engine自身のtau事象（tau表gがalpha challengeを読む対角）を評価する。`alphaRead T := reduceTriple (stageTriple deriveStage (alphaBase d) T)`、`engineTauBadEvent … G := {T | tauRead T ∈ productEvent d (zeroCheckBadSet d (G (alphaRead T)))}`、RFTの固定g事象は`G := fun _ => g`のスライス（`rft_tau_event_is_constant_slice`、rfl）。見出し `engine_tau_no_clash_mass_le`: **任意の**StrategyとGに対し P[engineTauBadEvent G ∩ stageNoClash deriveStage] ≤ tauTerm d（対角は定数を増やさない）。経路: RFTの3dカウンタ同時剥離を抽象的な段安定基底で言い直し（`tauFibreOn`/`tau_fibre_on_card`/`tau_target_card_on`）、alpha三つ組の値で分割して各ブロックでtargetが定数（alphaカウンタは全tauカウンタと素）、各値で採用済み`tau_product_mass_bound`、総和（`tau_diag_card`/`tau_diag_mass_le`）。engine自身のtau事象を用いた全体上界（`full_bad_draw_probability_le_engine_tau` と縮約/raw/戦略形）、RXLの経路を**再構築**した `raw_engine_full_bad_draw_probability_le ≤ combinedBound + 2·tauTerm + (22+5d+8)(22+5d+9)/2/|Block|`（衝突項1つ、余分なtauTermなし。任意のRoundCausal S・ClaimsCausal U・G）。**最終定理でengine自身の事象なのはtau（本モジュールの対角）と索引（RXL）**、alphaは∀ rows/coeffsOfのlane形（`prefixCoeffTable`はhashもproofも読まないので対角不要）、外側は∀-laneの`rawOuterBadEvent`で `SoundnessAssembly.outerBadEvent` ではない（alpha依存の外側対角はTSCCが固定プローバで行い、適応系列では**未解決**）。非空性: `sampleGate`（alpha=0で空、他で非空のbad集合: targetは実際にalphaで動く）、`alpha_read_is_gate_alpha_element`（RLTA経由）で `alphaRead` = 実現proofの `gateAlphaElement`、`engine_tau_event_at_realized_alpha` でengine自身のtau事象と一致（残る差は tables = s0.tables の結合のみ）、閉実例でRHS<1、定数表の検査。ROMでありkeccakではない、transcript制限付き因果的プローバ、CommittedTables結合は継承、系の健全性誤差ではない。 | 49 |

現行rootは140モデル・5469件の名付き定理です。
**ConditionalSoundnessの数値2^-184を系の健全性誤差として引用しないでください。**
これは外側sumcheckの一致事象のみを数えた値で、支配項であるWHIR/Merkleを含みません。
gate laneは証明されておらず、抽出とcommitmentの接合も仮定のままです。直近の検査結果はREPORTとmanifestで管理します。
件数は暗号安全性の達成率ではありません。

[スコープと未証明事項](SCOPE.md)、[結果・再現手順・次工程](REPORT.md)、
[全ソース対応表](wire3-manifest.json)を参照してください。
対応表はこのcheckoutに属する全Rust/Solidityファイルを列挙し、
モデルとの部分対応があるファイルと未対応のファイルを区別します。
依存ライブラリ・コンパイラを含む全コードを形式化したという意味ではありません。

## 実行

Lean 4.10.0の `lake` をPATHに入れ、リポジトリrootから実行します。

```sh
python3 -B mle/audit/test-check-wire3.py
python3 -B mle/audit/test-proof-dependencies.py
python3 -B mle/audit/test-provision-proof-dependencies.py
python3 -B mle/audit/provision-proof-dependencies.py
python3 -B mle/audit/check-wire3.py
```

検査は全current moduleのbuild、全名付き定理の実Lean環境での解決、推移的公理依存、
ソース/モデル/文書のhash、列挙漏れを確認します。空の検査リストは失敗です。
Mathlib 4.10と公式lockの6依存は全てcommit固定です。provisionのみが公開元から取得し、
既存依存の変更・不完全な取得は自動修復しません。guardはビルド前後に全5601 tracked fileを
Git blobと照合し、外部search path、追加Lean/config source、固定タグの不一致を拒否します。
通常LakeビルドのProofWidgets release成果物・生成済み.oleanの出自は、このsource検査では
証明しません。ソースのみからの全依存再生成とは区別します。
別途、40モデル時点の全非toolchain依存1370モジュールをfresh出力へ再生成し、全1019定理を
その出力だけで検査した記録をREPORTに保存しています。固定した11個の表示用JSデータと
Lean toolchainはその検査でも信頼境界に残り、後続追加モデルの検査範囲とは区別します。
fresh source再生成検査は `mle/audit/fresh-source-audit.py --run` で実行します（旧一時runnerの
再実装、self-check29件は `test-fresh-source-audit.py`）。全非toolchainモジュールを空の出力先へ
sourceから再compileし、既存`.lake/build`を検索pathに含めず、`lean --deps`で各moduleのimport解決を
確認し、全定理を再probeしてreceiptを `~/.local/share/wire3-fresh-source/` へ書きます。
`sorry`、`admit`、独自公理、`native_decide` による穴埋めは認めません。
許容するglobalな論理公理は `propext`、`Classical.choice`、`Quot.sound` のみです。
Poseidon/Cosetの全18表・1147語をSolidityと逐語比較し、PoseidonはRustとも比較します。
これは定数抽出の継続検査であり、コンパイラやソース全体の形式的同値性ではありません。

**公理リストが短くても、定理の引数やEngine観測は仮定として残ります。**
hash照合は変更検知であり、コードとLeanモデルの形式的同値性を証明しません。
未証明のWHIR健全性を `Engine.whirTail = true` で証明済みにすることもありません。
